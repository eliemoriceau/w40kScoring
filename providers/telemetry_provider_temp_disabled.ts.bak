// Temporarily disable telemetry provider to resolve module loading issues
// Will be re-enabled after fixing OpenTelemetry imports

import { ApplicationService } from '@adonisjs/core/types'

export default class TelemetryProvider {
  constructor(protected app: ApplicationService) {}

  register() {
    console.log('📊 Telemetry provider temporarily disabled during development')
  }

  async boot() {}
  async ready() {}
  async shutdown() {}
}